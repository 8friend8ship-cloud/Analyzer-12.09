# Browser-exposed variables. Only use keys that are safe to expose and restrict by HTTP referrer/provider policy.
VITE_YOUTUBE_API_KEY=

# Server-only variables. NEVER prefix secrets with VITE_.
CONTENT_OS_BACKEND_URL=
CONTENT_OS_BACKEND_TOKEN=
GEMINI_API_KEY=

# YouTube Analytics/Reporting should use approved OAuth/server-side flows rather than browser-bundled secrets.
